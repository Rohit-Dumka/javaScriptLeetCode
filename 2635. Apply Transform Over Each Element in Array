/**
 * @param {number[]} arr
 * @param {Function} fn
 * @return {number[]}
 */
var map = function(arr, fn) {
    const ansArr=[];
    for(let i=0;i<arr.length;i++)
        ansArr[i]=fn(arr[i],i);
    return ansArr;
};
